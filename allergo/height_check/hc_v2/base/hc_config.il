OUTPUT_PATH =
    "D:/program615/allegro/skill/allegro_skill/temp"

OUTPUT_FILENAME = "aaa"

QUERY_PARAM = list(
    list("PART_NUMBER"
         'iterate_part_number)

    list("DEVICE_TYPE"
         'iterate_device_type)

    list("REFDES"
         'iterate_refdes)

    list("MOUNT_SIDE"
         'iterate_mount_side)

    list("PACKAGE_HEIGHT_MAX_TOP"
         'iterate_installed_placebound_top)

    list("PACKAGE_HEIGHT_MAX_BOTTOM"
         'iterate_installed_placebound_bottom)
)
